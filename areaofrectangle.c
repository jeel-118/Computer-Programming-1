#include <stdio.h>
int main(){
int area;
int length;
printf("Enter length:");
scanf("%d",&length);
int breath;
printf("Enter breath:");
scanf("%d",&breath);
area =length*breath;
printf("%d",area);
return 0;
}
