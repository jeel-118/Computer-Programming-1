#include <stdio.h>
int main(){
int area;
int height;
printf("Enter height:");
scanf("%d",&height);
int length;
printf("Enter length:");
scanf("%d",&length);
area=height*(length*0.5);
printf("%d",area);
return 0;
}
